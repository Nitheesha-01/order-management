{
  "name": "order-management-ai",
  "version": "1.0.0",
  "description": "SAP CAP Order Management with AI Assistance",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@cap-js/sqlite": "^2.4",
    "@sap/cds": "^9",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "openai": "^6.45.0",
    "uuid": "^9.0.0"
  },
  "scripts": {
    "start": "cds run",
    "build": "cds build --production",
    "watch": "cds watch"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "url": "db.sqlite"
        }
      }
    }
  }
}
